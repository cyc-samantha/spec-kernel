{
  "name": "spec-kernel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Specification layer: a human intent in, a contract an agent team can claim out.",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
